<!--  This is the first step after starting.
 It chooses the mode for the download and conversion process.-->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#A020F0"
    >

    <TextView android:text="@string/choose_mode" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="25dp"
        android:textColor="#000000"/>

    <Button xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/button_auto"
        android:background="#96EA30"
        android:layout_width="fill_parent"
        android:layout_height="200dp"
        android:onClick="sendMessage"
        android:layout_marginTop="50dip"
        android:text="automatic mode"
        android:textColor="#000"
        android:textSize="50dp"/>

    <Button xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/button_manual"
        android:background="#96EA30"
        android:layout_width="fill_parent"
        android:layout_height="200dp"
        android:onClick="sendMessage"
        android:layout_marginTop="300dip"
        android:text="manual mode"
        android:textColor="#000000"
        android:textSize="50dp"/>

</RelativeLayout>
