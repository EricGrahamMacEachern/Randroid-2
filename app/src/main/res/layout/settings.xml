<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#0000FF"
    >

    <TextView android:text="@string/settings" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="35dp"
        android:textColor="#FFFFFF"
        android:layout_centerHorizontal="true"
/>

    <Button xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/choose_drive"
        android:background="#B0B0B0"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        android:onClick="sendMessage"
        android:layout_marginTop="50dip"
        android:text="choose drive"
        android:textColor="#000"
        android:textSize="25dp"/>

    <Button xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/choose_download_directory"
        android:background="#B0B0B0"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        android:onClick="sendMessage"
        android:layout_marginTop="175dip"
        android:text="choose download directory"
        android:textColor="#000"
        android:textSize="25dp"/>

    <Button xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/choose_download_limit"
    android:background="#B0B0B0"
    android:layout_width="fill_parent"
    android:layout_height="60dp"
    android:onClick="sendMessage"
    android:layout_marginTop="300dip"
    android:text="choose download limit"
    android:textColor="#000"
    android:textSize="25dp"/>

    <Button xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/start_conversion"
        android:background="#000000"
        android:layout_width="fill_parent"
        android:layout_height="60dp"
        android:onClick="sendMessage"
        android:layout_marginTop="425dip"
        android:text="start conversion and download process"
        android:textColor="#FFFFFF"
        android:textSize="25dp"/>


</RelativeLayout>